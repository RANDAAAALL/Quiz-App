import { StyleSheet } from "react-native";


const statusStyles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center" ,
    },
    textTile: {
        fontSize: 20,
        fontWeight: 600,
    }
});

export {statusStyles}